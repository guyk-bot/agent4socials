/** 3px brand gradient fixed above the nav. */
export default function LandingGradientBar() {
  return (
    <div
      className="funnel-gradient-bar pointer-events-none fixed left-0 right-0 top-0 z-[60] h-[3px]"
      aria-hidden
    />
  );
}
