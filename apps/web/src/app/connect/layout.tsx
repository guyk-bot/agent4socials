import React from 'react';

/** Minimal layout for OAuth consent pages (no dashboard sidebar). */
export default function ConnectLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
